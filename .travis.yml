sudo: true
language: c++
env:
  - DEBUG=1
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-5
      - ssh
      - cmake
      - catch
script:
  - mkdir build; cd build; cmake ../; make; sudo make Test

